import React from "react";
import HeadText from '../glamorous/HeadText';

const Loading = () => (<div className="no-albums-block">
        <HeadText>Loading...</HeadText>
      </div>);

export default Loading;
